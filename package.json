{
  "name": "@iexec-nox/nox-protocol-contracts",
  "version": "0.1.0-beta.4",
  "description": "Nox protocol smart contracts",
  "keywords": [
    "Nox",
    "Confidential DeFi",
    "cDeFi"
  ],
  "license": "Apache-2.0",
  "packageManager": "pnpm@10.28.2",
  "type": "module",
  "files": [
    "/contracts/interfaces/",
    "/contracts/sdk/",
    "/contracts/shared/"
  ],
  "scripts": {
    "prepare": "husky",
    "clean": "pnpm hardhat clean",
    "build": "pnpm hardhat build",
    "test": "dotenv -e .env.test -- pnpm hardhat test nodejs test/index.test.ts --network localhost",
    "test:unit": "pnpm hardhat test solidity",
    "test:integration": "dotenv -e .env.test -- pnpm hardhat test nodejs test/integration/index.test.ts --network localhost",
    "services:up": "dotenv -e .env.test -- sh -c 'docker compose -f docker-compose.test.yml up anvil -d --wait && rm -rf ignition/deployments/localhost && pnpm hardhat run scripts/deploy.ts --network localhost && pnpm hardhat run scripts/set-gateway.ts --network localhost && docker compose -f docker-compose.test.yml up'",
    "services:down": "docker compose -f docker-compose.test.yml down -v --remove-orphans",
    "coverage": "pnpm hardhat test solidity --coverage",
    "deploy": "pnpm hardhat run scripts/deploy.ts",
    "set-gateway": "pnpm hardhat run scripts/set-gateway.ts",
    "set-kms-public-key": "pnpm hardhat run scripts/set-kms-public-key.ts",
    "upgrade-acl": "pnpm hardhat run scripts/upgrade-acl.ts",
    "upgrade-nox-compute": "pnpm hardhat run scripts/upgrade-nox-compute.ts",
    "format": "pnpm prettier --write .",
    "format:check": "pnpm prettier --check ."
  },
  "lint-staged": {
    "*.{sol,js,ts,json,md,yml,yaml}": [
      "prettier --write"
    ]
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.4.0",
    "@openzeppelin/contracts-upgradeable": "^5.4.0",
    "encrypted-types": "^0.0.4"
  },
  "devDependencies": {
    "@iexec-nox/handle": "link:../nox-handle-sdk",
    "@nomicfoundation/hardhat-ignition": "^3.0.6",
    "@nomicfoundation/hardhat-toolbox-viem": "^5.0.1",
    "@types/node": "^22.8.5",
    "dotenv-cli": "^11.0.0",
    "forge-std": "github:foundry-rs/forge-std#v1.9.4",
    "hardhat": "^3.1.2",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7",
    "prettier": "^3.7.4",
    "prettier-plugin-solidity": "^2.2.1",
    "typescript": "~5.8.0",
    "viem": "^2.30.0"
  },
  "homepage": "https://github.com/iExec-Nox/nox-protocol-contracts#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/iExec-Nox/nox-protocol-contracts.git"
  },
  "bugs": {
    "url": "https://github.com/iExec-Nox/nox-protocol-contracts/issues"
  }
}
